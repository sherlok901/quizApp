namespace MRZS.Web
{
    public static class NullableHelper
    {
        //public Expression<Func<Book, bool>> IsDeleted()
        //{
        //    return Book => Book.IsDeleted == null || Book.IsDeleted == false;
        //}

        //public Expression<Func<EntityObject, bool>> IsDeleted()
        //{
        //    if(typeof(EntityObject).GetProperties().Any(p=>p.Name == "IsDeleted"))
        //    {
        //    PropertyInfo isDeletedPropety = typeof(EntityObject).GetProperty("IsDeleted");
        //    isDeletedPropety.
        //    }
        //    return Book => Book.IsDeleted == null || Book.IsDeleted == false;
        //}
    }
}